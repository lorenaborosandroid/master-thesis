<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/mediconear_white</item>
        <item name="colorPrimaryDark">@color/mediconear_white</item>
        <item name="colorAccent">@color/mediconear_purple</item>
        <item name="android:windowBackground">@drawable/background_splash</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/mediconear_white</item>
    </style>

</resources>
